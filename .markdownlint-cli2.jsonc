{
  // Rules live in .markdownlint.json (auto-loaded). This file only tells
  // markdownlint-cli2 to skip internal Superpowers process artifacts
  // (design specs / implementation plans), which are development records,
  // not shipped project documentation.
  "ignores": ["docs/superpowers/**"]
}
